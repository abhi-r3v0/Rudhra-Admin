<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_permissions"
            android:icon="@drawable/ic_action_home"
            android:title="Home" />
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_action_settings"
            android:title="Vulnerable settings" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_action_permissions"
            android:title="Sensitive data access" />
        <item
            android:id="@+id/nav_manage"
            android:icon="@drawable/ic_action_root"
            android:title="Root access" />
    </group>

    <item android:title="Contact us">
        <menu>
            <item
                android:id="@+id/nav_aboutus"
                android:icon="@drawable/ic_menu_send"
                android:title="About us" />
        </menu>
    </item>

</menu>
